server:
 port: 8080

spring:
 application:
  name: zuul-server

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:1099/eureka
  register-with-eureka: false
  fetchRegistry: true
  registryFetchIntervalSeconds: 2
  
zuul:
 sensitiveHeaders: