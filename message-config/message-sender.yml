message-sender:
 signature: ${spring.application.name}:${random.value}

server:
 port: 8080
 
message-printer:
 protocol: http://
 path: messages
 user: {cipher}4a58fd1d4fe0d0160c8de702102f1f953d8d854bfaed94610ac656fdffb16cdd
 pass: {cipher}bb9603b489e23313da8408b37f70aea8fb0f7448d3c046297a334f8b427cd35a
ribbon:
 ConnectTimeout: 1000
 ReadTimeout: 1000


hystrix:
 command:
  default:
   execution:
    isolation:
     thread:
      timeoutInMilliseconds: 2000
  circuitBreaker:
   requestVolumeThreshold: 4
   sleepWindowInMilliseconds: 50000
   errorThresholdPercentage: 50

eureka:
 client:
  serviceUrl:
   defaultZone: http://eureka-container:1099/eureka
  registryFetchIntervalSeconds: 2
 instance:
  instanceId: ${spring.application.name}:${spri‌​ng.application.insta‌​nce_id:${random.valu‌​e}}
  healthCheckUrlPath: /actuator/health
  statusPageUrl: /actuator/health